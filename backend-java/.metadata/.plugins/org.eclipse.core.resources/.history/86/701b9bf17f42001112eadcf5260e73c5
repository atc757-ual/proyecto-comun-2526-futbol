package com.futbol.player.feign.player;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

import dwsc.sampleproject.eureka.client.movie.feign.service.MovieFallback;


@FeignClient(name="CLIENT-PLAYER",fallback = PlayerFallback.class, configuration = FeignConfig.class)

public interface Player {

}
